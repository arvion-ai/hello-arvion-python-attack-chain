# Pinned to known-vulnerable lines. The first three lines mirror the
# proven-good versions from hello-arvion-python-multi-cve (which the
# Arvion CVE matcher catches reliably — 28 findings on first scan in
# that fixture). The remaining lines layer in more CVEs and the
# Jinja2 SSTI sink the app/main.py handler depends on.
#
# Each row is a finding the engine should emit:
#   - requests==2.27.0          → CVE-2023-32681 (Proxy-Authorization header leak)
#   - urllib3==1.26.0           → CVE-2023-43804 (Cookie header disclosure on redirect)
#   - cryptography==41.0.0      → CVE-2023-49083 (NULL deref in PKCS#12 load)
#   - jinja2==2.11.2            → CVE-2020-28493 (ReDoS) + CVE-2024-22195 (xmlattr SSTI)
#   - sqlalchemy==1.3.20        → CVE-2019-7548 (eval-injection in `relationship` extension)
#   - pyyaml==5.3.1             → CVE-2020-14343 (full_load RCE)
#   - python-multipart==0.0.5   → CVE-2024-24762 (DoS via header line splitting)
#   - aiohttp==3.8.1            → CVE-2023-37276 (HTTP request smuggling)
#
# fastapi + uvicorn pinned to stable versions so the manifest is
# realistic; they're not the demo targets.
fastapi==0.108.0
uvicorn==0.27.0
requests==2.27.0
urllib3==1.26.0
cryptography==41.0.0
jinja2==2.11.2
sqlalchemy==1.3.20
pyyaml==5.3.1
python-multipart==0.0.32
aiohttp==3.8.1
boto3==1.26.0
